from django.urls import path
from . import views

app_name = 'users'

urlpatterns = [
    # path('app/users', views.users, name='users'),
    # path('app/users/add', views.add_user, name='users.add'),
]